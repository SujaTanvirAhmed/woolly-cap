import Typography from '@mui/material/Typography';

export default function Loader() {
    return (
        <Typography
            variant="h6"
            sx={{ color: "dodgerblue" }}
        >LOADING ...</Typography>
    );
}